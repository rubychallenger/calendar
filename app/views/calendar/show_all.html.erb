<%= render 'header' %>
<div class="row">
	<div class="small-12 columns">
		<ul class="small-block-grid-3 medium-block-grid-4 large-block-grid-5 imagegrid">
			<% @all.each do |e| %>
				<li class="titleadmin">
					<%= link_to image_tag(e.picture), edit_title_path(e) %>
				</li>
			<% end %>
		</ul>
	</div>
</div>